const { partOne, partTwo } = require("./day2");

describe("2017 -- DAY 2", () => {
  it("PART 1 -- ", () => {
    partOne("fixture.txt", response => {
      expect(response).toBe(18);
    });
  });

  it("PART 2 --", () => {});
});
